class Question {
  String questionText;
  bool questionAnswer;

  Question({String questionText, bool questionsAnswer}) {
    this.questionText = questionText;
    this.questionAnswer = questionsAnswer;
  }
}
